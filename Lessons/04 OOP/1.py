# Additional Constructors

import datetime


# @classmethod
# def fromtimestamp(cls, t):
#     # "Construct a date from a POSSIX timestamp(like time.time())"
#     y, m, d, hh, mm, weekday, jday, dst = _time.localtime(t)
#     return cls(y, m, d)
